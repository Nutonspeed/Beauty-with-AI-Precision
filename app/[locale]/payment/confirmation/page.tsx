'use client';
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import Link from 'next/link'
import { CheckCircle, Download, Calendar } from 'lucide-react';

export default function PaymentConfirmationPage() { // Mock payment data const payment = { id: 'PAY-2024-001', amount: 2900, method: 'พร้อมเพย์', status: 'success', timestamp: new Date().toISOString(), plan: 'Professional', features: [ 'วิเคราะห์ผิวหนัดด้วย AI ไม่จำกัด', 'บันทึกผลลัพธ์ได้ไม่จำกัด', 'รายงานแบบละเอียด', 'สิทธิ์ใช้งานฟีเจอร์พิเศษ', 'การสนับสนุนลูกค้าแบบ Priority' ] } const handleDownloadReceipt = () => { // Mock download const link = document.createElement('a') link.href = '/receipts/sample-receipt.pdf' link.download = `receipt-${payment.id}.pdf` link.click() } return ( <div className="container mx-auto py-8 px-4"> <div className="max-w-2xl mx-auto"> {/* Success Message */} <div className="text-center mb-8"> <CheckCircle className="w-16 h-16 text-green-500 mx-auto mb-4" /> <h1 className="text-3xl font-bold mb-2">การชำระเงินสำเร็จ!</h1> <p className="text-gray-600">ขอบคุณที่เลือกใช้บริการของเรา</p> </div> {/* Payment Details */} <Card className="mb-6"> <CardHeader> <CardTitle className="flex items-center justify-between"> <span>รายละเอียดการชำระเงิน</span> <Badge variant="secondary" className="text-green-600"> {payment.status === 'success' ? 'สำเร็จ' : 'รอดำเนินการ'} </Badge> </CardTitle> </CardHeader> <CardContent> <div className="space-y-3"> <div className="flex justify-between"> <span className="text-gray-600">หมายเลขการชำระ:</span> <span className="font-mono">{payment.id}</span> </div> <div className="flex justify-between"> <span className="text-gray-600">แพ็คเกจ:</span> <span>{payment.plan}</span> </div> <div className="flex justify-between"> <span className="text-gray-600">วิธีการชำระ:</span> <span>{payment.method}</span> </div> <div className="flex justify-between"> <span className="text-gray-600">จำนวนเงิน:</span> <span className="text-2xl font-bold text-green-600"> ฿{payment.amount.toLocaleString()} </span> </div> <div className="flex justify-between"> <span className="text-gray-600">วันที่ชำระ:</span> <span>{new Date(payment.timestamp).toLocaleDateString('th-TH')}</span> </div> </div> </CardContent> </Card> {/* Plan Features */} <Card className="mb-6"> <CardHeader> <CardTitle>สิทธิประโยชน์ที่ได้รับ</CardTitle> </CardHeader> <CardContent> <ul className="space-y-2"> {payment.features.map((feature, index) => ( <li key={index} className="flex items-center space-x-2"> <CheckCircle className="w-4 h-4 text-green-500" /> <span>{feature}</span> </li> ))} </ul> </CardContent> </Card> {/* Actions */} <div className="flex flex-col sm:flex-row gap-4"> <Button onClick={handleDownloadReceipt} className="flex-1"> <Download className="w-4 h-4 mr-2" /> ดาวน์โหลดใบเสร็จ </Button> <Button variant="outline" asChild className="flex-1"> <Link href="/th/analysis"> เริ่มใช้งาน </Link> </Button> </div> {/* Support */} <div className="mt-8 p-4 bg-gray-50 rounded-lg"> <p className="text-center text-sm text-gray-600"> มีปัญหาใช้งาน? <Link href="/th/support" className="text-blue-600 hover:underline ml-1"> ติดต่อฝ่ายสนับสนุน </Link> </p> </div> </div> </div> )
}