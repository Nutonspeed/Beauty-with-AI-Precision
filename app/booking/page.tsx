// Re-export from [locale] folder for backward compatibility
// This allows /booking to work while keeping the main implementation in /[locale]/booking
export { default } from '@/app/[locale]/booking/page'
