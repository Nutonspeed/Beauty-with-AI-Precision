export async function register() {
  // Temporarily disabled to speed up dev server startup
  // TODO: Re-enable Sentry after fixing import performance
  console.info('[instrumentation] Disabled for faster development')
  return
}
