# 🔍 คู่มือตรวจสอบข้อผิดพลาดข้อความ (Manual)

## 🎯 ขั้นตอนการตรวจสอบ

### **1. เปิด VS Code**
```bash
cd d:\127995803\Beauty-with-AI-Precision
code .
```

### **2. ใช้ Search & Replace ทั่วโปรเจค**

**กด `Ctrl+Shift+F`** เพื่อเปิด Search ทั่วโปรเจค

---

## 📋 รายการที่ต้องตรวจสอบ

### **สัญลักษณ์ซ้ำ**

| ค้นหา | แทนที่ | ตัวอย่าง |
|--------|---------|---------|
| `฿฿` | `฿` | ฿฿2,900 -> ฿2,900 |
| `$$` | `$` | $$100 -> $100 |
| `%%` | `%` | 50%% -> 50% |
| `##` | `#` | ##header -> #header |
| `***` | `**` | ***bold*** -> **bold** |
| `!!` | `!` | !!error -> !error |
| `??` | `?` | ??question -> ?question |

### **คำซ้ำภาษาไทย**

| ค้นหา | แทนที่ | หมายเหตุ |
|--------|---------|---------|
| `\bว่า\s+ว่า\b` | `ว่า` | ว่าว่า -> ว่า |
| `\bทำ\s+ทำ\b` | `ทำ` | ทำทำ -> ทำ |
| `\bได้\s+ได้\b` | `ได้` | ได้ได้ -> ได้ |
| `\bมี\s+มี\b` | `มี` | มีมี -> มี |
| `\bจะ\s+จะ\b` | `จะ` | จะจะ -> จะ |

### **Spacing ผิด**

| ค้นหา | แทนที่ | หมายเหตุ |
|--------|---------|---------|
| `[ ]{3,}` | ` ` | ช่องว่าง 3+ ตัว -> 1 ตัว |
| `\s+$` | `` | ลบช่องว่างท้ายบรรทัด |
| `\n\s*\n\s*\n` | `\n\n` | ลบบรรทัดว่างซ้ำ |

### **ตัวพิมพ์ผิดทั่วไป**

| ค้นหา | แทนที่ | หมายเหตุ |
|--------|---------|---------|
| `ทำงานต่อ` | `ทำงานต่อ` | ตัวพิมพ์ภาษาอังกฤษ |
| `ไม่ได้` | `ไม่ได้` | ตัวพิมพ์ภาษาอังกฤษ |
| `ดังนั้น` | `ดังนั้น` | ตัวพิมพ์ภาษาอังกฤษ |

---

## 🚀 วิธีทำ (Step-by-Step)

### **1. แก้ไข ฿฿**
```
1. Ctrl+Shift+F
2. ค้นหา: ฿฿
3. กด Ctrl+Shift+H (Replace in Files)
4. Replace: ฿
5. กด Replace All
```

### **2. แก้ไขคำซ้ำ**
```
1. Ctrl+Shift+F
2. ค้นหา: ว่า\s+ว่า (ใช้ Regex)
3. กด Ctrl+Shift+H
4. Replace: ว่า
5. กด Replace All
```

### **3. ตรวจสอบผลลัพธ์**
```
1. เปิดไฟล์ที่แก้ไข
2. ตรวจสอบด้วยตา
3. ถูกต้องแล้ว? บันทึก
4. มีปัญหา? Undo (Ctrl+Z)
```

---

## 📁 ไฟล์ที่ควรตรวจสอบก่อน

### **ควรตรวจสอบก่อน:**
- `app/[locale]/pricing/page.tsx` - หน้าราคา (มี ฿ เยอะ)
- `components/payment/` - คอมโพเนนท์การชำระเงิน
- `app/[locale]/payment/` - หน้าการชำระเงิน
- `components/ui/` - UI components ทั่วไป

### **ตรวจสอบทุกไฟล์:**
- ไฟล์ `.tsx` ทั้งหมด
- ไฟล์ `.ts` ทั้งหมด
- ไฟล์ `.js` ทั้งหมด

---

## ⏱️ ใช้เวลาประมาณ

| งาน | เวลา |
|-----|------|
| แก้ไข ฿฿ | 5 นาที |
| แก้ไขคำซ้ำ | 10 นาที |
| แก้ไข spacing | 10 นาที |
| ตรวจสอบผล | 15 นาที |
| **รวม** | **40 นาที** |

---

## ✅ เช็คลิสต์หลังแก้ไข

- [ ] ไม่มี ฿฿ ในโปรเจค
- [ ] ไม่มีคำซ้ำ (ว่าว่า, ทำทำ)
- [ ] ไม่มีช่องว่างเกิน 3 ตัว
- [ ] ไม่มีช่องว่างท้ายบรรทัด
- [ ] Build ผ่าน (`pnpm build`)
- [ ] Lint ผ่าน (`pnpm lint`)

---

## 💡 Tips

1. **Backup ก่อนแก้ไข**
   ```bash
   git add -A
   git commit -m "Before text error fixes"
   ```

2. **ทีละประเภท**
   - อย่าแก้หลายอย่างพร้อมกัน
   - แก้ไข ฿฿ ให้หมดก่อน
   - ค่อยๆ ตรวจสอบ

3. **ใช้ Regex เมื่อจำเป็น**
   - กด `.*` เปิดใช้ Regex
   - ระวังผลลัพธ์ที่ไม่ต้องการ

4. **Undo ได้เสมอ**
   - `Ctrl+Z` ถ้าแก้ผิด
   - `git checkout -- .` ถ้าผิดมาก

---

## 🎯 เริ่มเลย!

1. เปิด VS Code
2. กด `Ctrl+Shift+F`
3. ค้นหา `฿฿`
4. แก้ไข!

**🔥 ทำได้เลย! ไม่ยาก!**
