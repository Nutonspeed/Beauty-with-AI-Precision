# 🔍 วิธีเช็คฐานข้อมูล Supabase ก่อนรันการ Migration

## ❌ ปัญหาที่พบ
ไม่สามารถติดตั้ง `pg` module ได้เนื่องจาก dependency conflicts ในโปรเจกต์

## ✅ วิธีแก้: เช็คผ่าน Supabase SQL Editor

### ขั้นตอนที่ 1: เปิด SQL Editor
1. ไปที่ https://supabase.com/dashboard/project/bgejeqqngzvuokdffadu/sql/new
2. Login ด้วย Supabase account ของคุณ

### ขั้นตอนที่ 2: รันคำสั่ง SQL เหล่านี้ทีละคำสั่ง

#### 🔹 คำสั่งที่ 1: เช็คตารางทั้งหมด
\`\`\`sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
  AND table_type = 'BASE TABLE'
ORDER BY table_name;
\`\`\`
**ผลที่คาดหวัง:** รายชื่อตารางทั้งหมดในฐานข้อมูล

---

#### 🔹 คำสั่งที่ 2: เช็ค users table มีหรือไม่
\`\`\`sql
SELECT 
  COUNT(*) as total_rows,
  COUNT(DISTINCT clinic_id) as unique_clinic_ids,
  COUNT(CASE WHEN clinic_id IS NOT NULL THEN 1 END) as rows_with_clinic_id
FROM users;
\`\`\`
**ผลที่คาดหวัง:** จำนวน users, จำนวน clinic_id ที่ไม่ซ้ำ

---

#### 🔹 คำสั่งที่ 3: ดู clinic_id ทั้งหมดใน users
\`\`\`sql
SELECT 
  clinic_id,
  COUNT(*) as user_count
FROM users
WHERE clinic_id IS NOT NULL
GROUP BY clinic_id
ORDER BY user_count DESC;
\`\`\`
**ผลที่คาดหวัง:** รายการ clinic_id และจำนวน users ที่ใช้แต่ละ clinic

**⚠️ CRITICAL:** ถ้าเห็น `00000000-0000-0000-0000-000000000001` ในรายการนี้แสดงว่ามี users ที่อ้างถึง clinic ที่ยังไม่มี

---

#### 🔹 คำสั่งที่ 4: เช็ค clinics table
\`\`\`sql
SELECT COUNT(*) as row_count FROM clinics;
\`\`\`
**ผลที่คาดหวัง:** 
- ถ้า error แสดงว่า table ไม่มี ✅ (นี่คือสาเหตุของปัญหา!)
- ถ้าได้ตัวเลข แสดงว่า table มีแล้ว

---

#### 🔹 คำสั่งที่ 5: เช็ค foreign keys ใน users
\`\`\`sql
SELECT
  tc.constraint_name,
  kcu.column_name,
  ccu.table_name AS references_table,
  ccu.column_name AS references_column
FROM information_schema.table_constraints AS tc
JOIN information_schema.key_column_usage AS kcu
  ON tc.constraint_name = kcu.constraint_name
JOIN information_schema.constraint_column_usage AS ccu
  ON ccu.constraint_name = tc.constraint_name
WHERE tc.table_schema = 'public'
  AND tc.table_name = 'users' 
  AND tc.constraint_type = 'FOREIGN KEY';
\`\`\`
**ผลที่คาดหวัง:** รายการ FK constraints ที่มีอยู่ใน users table

---

#### 🔹 คำสั่งที่ 6: ดู sample data จาก users
\`\`\`sql
SELECT 
  id,
  email,
  clinic_id,
  created_at
FROM users
WHERE clinic_id IS NOT NULL
ORDER BY created_at DESC
LIMIT 10;
\`\`\`
**ผลที่คาดหวัง:** รายการ users 10 คนล่าสุดที่มี clinic_id

---

### ขั้นตอนที่ 3: บันทึกผลลัพธ์

**กรุณาบันทึกผลลัพธ์จากคำสั่งเหล่านี้:**

1. ✅ มีตารางอะไรบ้างในฐานข้อมูล?
   \`\`\`
   [พิมพ์รายชื่อตารางที่พบ เช่น users, chat_history, ...]
   \`\`\`

2. ✅ users table มีกี่ rows? มี clinic_id กี่ค่า?
   \`\`\`
   total_rows: _____
   unique_clinic_ids: _____
   rows_with_clinic_id: _____
   \`\`\`

3. ✅ มี clinic_id อะไรบ้างใน users?
   \`\`\`
   [คัดลอกผลลัพธ์จาก query ที่ 3]
   \`\`\`

4. ✅ clinics table มีหรือไม่?
   - [ ] ไม่มี (error เมื่อรัน query)
   - [ ] มี (มี ___ rows)

5. ✅ มี FK constraints อะไรบ้างใน users?
   \`\`\`
   [คัดลอกผลลัพธ์จาก query ที่ 5]
   \`\`\`

---

## 📊 วิเคราะห์ผลลัพธ์

### กรณีที่ 1: clinics table ไม่มี (คาดว่าเป็นกรณีนี้)
**ปัญหา:** users table มี clinic_id แต่ clinics table ยังไม่ได้สร้าง
**วิธีแก้:**
1. สร้าง clinics table ก่อน
2. Insert clinic record ที่มี id = `00000000-0000-0000-0000-000000000001`
3. แล้วค่อยเพิ่ม FK constraint

### กรณีที่ 2: clinics table มีแต่ว่าง
**ปัญหา:** clinics table ว่างเปล่า ไม่มี id ที่ users อ้างถึง
**วิธีแก้:**
1. Insert clinic record ที่มี id ตรงกับที่ users ใช้อยู่
2. แล้วค่อยเพิ่ม FK constraint

### กรณีที่ 3: ทุกอย่างมีครบแล้ว
**ปัญหา:** อาจมี FK constraint อยู่แล้ว หรือตารางอื่นๆ สร้างแล้ว
**วิธีแก้:**
1. ข้าม migration ส่วนที่มีแล้ว
2. รันเฉพาะส่วนที่ยังไม่มี

---

## 🔄 ขั้นตอนถัดไป

หลังจากรัน queries เหล่านี้แล้ว:

1. ✅ แชร์ผลลัพธ์มาให้ AI ดู
2. ✅ AI จะปรับ migration script ให้เหมาะสม
3. ✅ รัน migration ใหม่
4. ✅ ทดสอบว่าใช้งานได้

---

## 💡 เคล็ดลับ

- ใช้ปุ่ม "Run" ใน SQL Editor เพื่อรันคำสั่งทีละคำสั่ง
- ถ้าคำสั่งไหน error ไม่ต้องตกใจ บอกมาได้เลย
- Screenshot ผลลัพธ์แนบมาได้ถ้าสะดวก
- ถ้าไม่แน่ใจ ให้รันคำสั่งที่ 1, 2, 4 อย่างน้อย (สำคัญที่สุด)
