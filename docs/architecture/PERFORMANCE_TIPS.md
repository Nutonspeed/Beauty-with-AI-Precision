# 🚀 Performance Optimization Guide

## คอมพิวเตอร์ช้า? แก้ปัญหาได้แล้ว!

### ✅ สิ่งที่ทำแล้ว:
1. ✨ เพิ่ม `.vscode/settings.json` สำหรับ optimize VS Code
2. 🎯 ปิด file watchers ที่ไม่จำเป็น (node_modules, .next, etc.)
3. 💾 จำกัด TypeScript memory usage
4. ⚡ ปิด auto-update extensions

---

## 🔧 แนะนำเพิ่มเติม:

### 1. ปิด Dev Server ที่ไม่ใช้:
```powershell
# ตรวจสอบ process ที่รัน
Get-Process node, python | Select Name, Id, CPU

# ปิด process ที่ไม่ใช้ (ระบุ Id)
Stop-Process -Id <process-id>
```

### 2. เคลียร์ Cache:
```powershell
# Next.js cache
Remove-Item -Recurse -Force .next
Remove-Item -Recurse -Force node_modules/.cache

# TypeScript cache
Remove-Item -Recurse -Force node_modules/.tmp
```

### 3. ปิด Chrome/Edge Tabs ที่ไม่ใช้:
- Chrome กิน memory 98 MB
- Edge กิน memory มากกว่า 200 MB
- ✅ แนะนำปิด tabs ที่เปิดค้างไว้

### 4. รีสตาร์ท VS Code:
```powershell
# ปิด VS Code ทั้งหมด
Get-Process Code | Stop-Process -Force

# เปิดใหม่
code .
```

### 5. Windows Performance Tips:
```powershell
# เคลียร์ memory
Clear-RecycleBin -Force

# ตรวจสอบ disk space
Get-PSDrive C | Select Used, Free, @{n="FreeGB";e={[math]::Round($_.Free/1GB,2)}}
```

---

## 📊 Memory Usage ปัจจุบัน:

| Process | Memory (MB) | แนะนำ |
|---------|-------------|-------|
| VS Code (Main) | 1,117 MB | ⚠️ สูงมาก - ควรรีสตาร์ท |
| Memory Compression | 859 MB | ℹ️ ปกติ (Windows) |
| VS Code (Extensions) | 285 MB | ✅ ปกติ |
| Edge/Chrome | 220+ MB | ⚠️ ปิด tabs ที่ไม่ใช้ |

---

## ⚡ Quick Wins (ทำได้เลย):

### A. ปิดไฟล์ที่ไม่ใช้ใน VS Code:
- File → Close All Editors
- หรือ `Ctrl+K W`

### B. Restart TypeScript Server:
- `Ctrl+Shift+P` → "TypeScript: Restart TS Server"

### C. ปิด Terminal ที่ไม่ใช้:
- คุณมี terminal เปิดอยู่ 27+ tabs!
- ปิดที่ไม่ใช้: Right click → Kill Terminal

### D. Disable Extensions ชั่วคราว:
Extensions ที่อาจทำให้ช้า:
- Git Graph
- ESLint (ถ้าไม่ได้ใช้)
- Prettier (ถ้าไม่ได้ใช้)
- Live Server
- Auto Complete

---

## 🎯 แนะนำสำหรับโปรเจคนี้:

### เปิดแค่ไฟล์ที่ใช้งานจริง:
```
ไฟล์สำคัญที่ต้องเปิด:
├── app/                    (ตอนแก้ UI)
├── lib/                    (ตอนแก้ logic)
├── components/             (ตอนแก้ components)
└── .env.example           (ดู config)

❌ ไม่ต้องเปิด:
├── node_modules/
├── .next/
├── services/ai-service/  (แยก project)
└── docs/                 (เปิดเวลาต้องการ)
```

---

## 🔄 Maintenance Schedule:

**ทุกวัน:**
- ✅ ปิด VS Code ตอนเลิกงาน
- ✅ Close All Editors ก่อนเปลี่ยน task

**ทุกสัปดาห์:**
- 🗑️ เคลียร์ `.next` cache
- 🔄 รีสตาร์ทคอม
- 🧹 ลบ test files ที่ไม่ใช้

**ทุกเดือน:**
- 📦 `pnpm store prune` (เคลียร์ pnpm cache)
- 🔍 ตรวจสอบ disk space
- 🆙 Update VS Code & Extensions

---

## 📞 ถ้ายังช้าอยู่:

1. **Restart คอมพิวเตอร์** (ง่ายที่สุด!)
2. **เพิ่ม RAM** (แนะนำ 16GB+)
3. **ใช้ SSD** (ถ้ายังใช้ HDD อยู่)
4. **ปิด Antivirus Scan** สำหรับโฟลเดอร์โปรเจค

---

**สร้างเมื่อ:** November 9, 2025  
**อัพเดท:** ตามความจำเป็น
